#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, const char *argv[])
{
	char a[] = {"I love you!"},m[10];
	int len = strlen(a);

	char b,c=0;
	for(int i = 0; i<len; i++){
		if(a[i]==' '){
			m[c++;
			num++;
		}

	}
	printf("%s\n",a);
	return 0;
}
